<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Open on Telegram</title>
  <meta name="description" content="Click the button to talk to me on Telegram." />
  <meta name="theme-color" content="#101827" />
  <meta property="og:title" content="Open on Telegram" />
  <meta property="og:description" content="Click the button to talk with me on Telegram." />
  <meta property="og:type" content="website" />
  <meta property="og:image" content="" />
  <meta name="robots" content="noindex,follow" />

  <style>
    :root { --bg: #0b1220; --fg: #e5e7eb; --muted:#94a3b8; --btn:#22c55e; --btnh:#16a34a; }
    * { box-sizing: border-box; }
    html, body { height: 100%; }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, "Helvetica Neue", Arial, sans-serif;
      color: var(--fg);
      background: radial-gradient(1200px 800px at 50% -10%, #1f2937 0%, #0b1220 60%, #070b14 100%);
      display: grid;
      place-items: center;
    }
    .card {
      width: min(92vw, 560px);
      background: rgba(17, 24, 39, 0.75);
      border: 1px solid rgba(148, 163, 184, 0.15);
      border-radius: 24px;
      padding: 28px;
      box-shadow: 0 10px 30px rgba(0,0,0,.35);
      text-align: center;
      backdrop-filter: blur(6px);
    }
    h1 { font-size: clamp(22px, 3vw, 28px); margin: 6px 0 12px; }
    p { margin: 0 0 18px; color: var(--muted); }
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      width: 100%;
      padding: 16px 18px;
      font-weight: 700;
      font-size: 16px;
      letter-spacing: 0.3px;
      color: white;
      background: linear-gradient(180deg, var(--btn), #1fb154);
      border: 0;
      border-radius: 16px;
      cursor: pointer;
      box-shadow: 0 8px 20px rgba(34, 197, 94, .35);
      transition: transform .06s ease, box-shadow .2s ease, filter .2s ease;
    }
    .btn:hover { filter: brightness(1.02); box-shadow: 0 10px 26px rgba(34, 197, 94, .4); }
    .btn:active { transform: translateY(1px); }
    .hint { margin-top: 14px; font-size: 13px; color: var(--muted); }
    .link { color: #60a5fa; text-decoration: none; }
    .link:hover { text-decoration: underline; }
    .copybox { user-select: all; word-break: break-all; padding: 10px 12px; font-size: 12px; background: #0b1220; border: 1px dashed rgba(148, 163, 184, .35); border-radius: 12px; margin-top: 10px; }
    footer { margin-top: 16px; font-size: 11.5px; color: #6b7280; }
  </style>

  <script>
    // ====== CONFIG ======
    const TELEGRAM_URL = "https://t.me/+o4VZafff0vFlZGJh"; // seu canal do Telegram
    const META_PIXEL_ID = "672982815142267";               // seu Pixel do Meta Ads

    function withQueryParams(baseUrl) {
      try {
        const qs = window.location.search;
        if (!qs) return baseUrl;
        return baseUrl + (baseUrl.includes("?") ? "&" : "?") + qs.slice(1);
